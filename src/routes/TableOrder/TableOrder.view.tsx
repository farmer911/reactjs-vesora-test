import React from "react";
import HeadlineTable from "../../components/HeadlineTable";

const TableOrder = (): JSX.Element => {
    return (
        <HeadlineTable />
    )
}

export default React.memo(TableOrder);
